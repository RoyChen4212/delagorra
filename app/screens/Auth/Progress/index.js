import React from 'react';

import * as Styled from './styles';

const Progress = () => (
  <Styled.Box flex={1} bg="background">
    <Styled.Indicator />
  </Styled.Box>
);

export default Progress;
